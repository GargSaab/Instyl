import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    marginHorizontal: 15,
    alignItems: "center",
  },
  textInput: {
    flex: 1,
    marginHorizontal: 15,
    fontSize: 20,
  },
});

export default styles;
